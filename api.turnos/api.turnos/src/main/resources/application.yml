application:
  kafka-values:
    bootstrap-servers: PLAINTEXT://localhost:19092

quarkus:
  http:
    port: 8085

  datasource:
    username: financebank
    password: financebank123
    jdbc:
      url: jdbc:postgresql://${DB_HOST:localhost:5432}/${DB_NAME:db_turnos}

  hibernate-orm:
    database:
      generation: drop-and-create
    log:
      sql: false
      format-sql: true
      bind-parameters: false